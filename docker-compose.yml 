version: '3.9'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock  # necessário para criar containers
    depends_on:
      - sandbox_build

  sandbox_build:
    build:
      context: ./docker
      dockerfile: Dockerfile
    image: sandbox_extractor_image
